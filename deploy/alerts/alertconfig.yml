apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: team-sas-alerts-slack
  namespace: tbd
  labels:
    team: tbd
    alertmanagerConfig: team-sas-alerts-slack
spec:
  receivers:
    - name: team-sas-alerts-receiver
      slackConfigs:
        - apiURL:
            key: apiUrl
            name: slack-webhook
          channel: "#team-sas-alerts"
          iconEmoji: ":small_airplane:"
          username: "Varsling"
          sendResolved: true
          title: |-
            [{{ .Status | toUpper }}{{ if eq .Status "firing" }}:{{ .Alerts.Firing | len }}{{ end }}] {{ .CommonLabels.alertname }}
          text: >-
            {{ range .Alerts -}}
            *Hva:* {{ .Annotations.hva }}{{ "\n" }}
            *Handling:* {{ .Annotations.handling }}
            {{ end }}
          color: |-
            {{ if eq .Status "firing" -}}
              {{ if eq .CommonLabels.severity "warning" -}}
                warning
              {{- else if eq .CommonLabels.severity "fatal" -}}
                #611f69
              {{- else if eq .CommonLabels.severity "critical" -}}
                #611f69
              {{- else if eq .CommonLabels.severity "danger" -}}
                danger
              {{- else if eq .CommonLabels.severity "error" -}}
                danger
              {{- else if eq .CommonLabels.severity "notice" -}}
                good
              {{- else if eq .CommonLabels.severity "info" -}}
                #36c5f0
              {{- else -}}
                .CommonLabels.severity
              {{- end -}}
            {{ else -}}
            good
            {{- end }}
  route:
    groupBy:
      - alertname
    matchers:
      - name: "team_sas_alerts"
        matchType: "="
        value: "slack"
    groupInterval: 10s
    groupWait: 5s
    receiver: team-sas-alerts-receiver
    repeatInterval: 2m
